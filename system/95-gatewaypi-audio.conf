# GatewayPi: realtime scheduling and locked memory for the audio group.
# Installed to /etc/security/limits.d/ — applied to the kiosk session via
# PAMName=login in gatewaypi-kiosk.service.
@audio - rtprio 95
@audio - memlock unlimited
@audio - nice -19
